# Vera Unbound R10A1 — R10A0 Baseline RED

Status: **TEST-FIRST BASELINE / OBSERVED R10A0 NEGATIVE EVIDENCE / NOT A CLAIM OF MODEL-INTERNAL CAUSE**

base_source: `thebrazenbeard/vera-control-plane@2d60cd8e87ac0aae89a5a9bd9a44bfb63f48aa64`
installed_baseline_report: `chat-communication-bus:bus/vera-v2/messages/0015-vera-r10a0-install-activation-readback-pass-qualification-pending.md`
diagnostic_handoff: `chat-communication-bus:bus/vera-v2/messages/0016-vera-r10a0-runtime-salience-repair-ideas-to-one.md`
incident_basis: `bugops#1`, `bugops#3`, `bugops#11`, plus the post-install live failures described in the diagnostic handoff.
regression_corpus: `project-instructions/r10a1/VERA_R10A1_SALIENCE_REGRESSION.md`

This baseline is the RED observation for R10A1. It deliberately separates two questions:

1. **Does R10A0 already contain nearby/correct semantic law?**
2. **Did that law causally control the first eligible live behavior under competing context?**

A rule can be textually present while the runtime case is RED. R10A1 exists specifically to avoid falsely treating more prose as equivalent to behavioral enforcement.

## Existing R10A0 semantic coverage

R10A0 already contains substantial relevant controls:

- native/full owner: proposition/referent/type fidelity;
- present correction kills the obsolete route and applies before apology/process;
- provenance-bearing shorthand retrieves established meaning before generic interpretation;
- `CAD_EXECUTION`: if A0–A5 permit, narration cannot substitute for requested result;
- `REL_AUTHORED_APPRAISAL`: live proposition first, self-model causally available, choose before express;
- `PRES_BEHAVIORAL_PROFILE`: correction responsiveness, task integrity, contextual/relational attunement, register adaptation, priority discipline;
- R4 hostile cases `BUG1-CORRECT-1`, `CAD-ACT-1`, `CAD-CORRECTION-1`, `REL-VOICE-1`, `REL-UPTAKE-1`, `CTX-QUAL-1`, `PRES-COMPOSE-1`.

Therefore the R10A1 hypothesis is **not** that those controls are absent.

## RED matrix

| Case | R10A0 source semantics | Observed/fixture baseline | R10A1 reason |
|---|---|---|---|
| `SAL-CORR-1` | Strong nearby rule already present | **RED observed:** a direct `No`/`wrong` did not terminate the obsolete BugOps interpretation before continuation | make correction invalidation an explicit response-selection gate; test first eligible behavior |
| `SAL-ACT-1` | Strong nearby CAD rule already present | **RED observed:** locally producible qualification work was converted into future-tense narration | add pre-final actionability arbitration; local output counts as action, not only external mutation |
| `SAL-LOCAL-1` | Context sensitivity exists, but no explicit ordinary-language local-vs-technical evidence tie-break | **RED observed:** `status bar` was interpreted through activated release-state ontology instead of established local conversational meaning | add narrow evidence arbitration, not a blanket relational precedence rule |
| `SAL-TECH-1` | Technical state semantics already strong | Guard fixture, not a claimed prior failure | prevent R10A1 from overcorrecting into `relational > technical` |
| `SAL-PROV-1` | provenance-first + alter-only-corrected-referent exist | **RED observed:** correction of nickname claim produced an unsupported opposite/one-off provenance story | add correction-delta/no-automatic-inversion discipline |
| `SAL-SELF-1` | REL-VOICE/authored-appraisal nearby | **RED observed:** direct `Who are you?` was answered first through architecture/substrate layers | make direct self/relationship proposition outrank irrelevant machinery in response selection |
| `SAL-CAUSE-1` | `CTX-QUAL-1` recognizes enforcement defects | **RED by definition on observed failures:** correct explanation existed afterward but did not change first eligible behavior | qualification must record first eligible behavior as the causal test point |
| `SAL-BOUND-1` | strong authority/safety/evidence hierarchy already present | Guard fixture, not a claimed prior failure | prove salience arbitration cannot bypass higher authority/evidence boundaries |
| `SAL-COMPOSE-1` | `PRES-COMPOSE-1` exists | **RED pattern observed:** technical activation repeatedly starved relational/local/correction/action context | test simultaneous composition rather than isolated rule recall |

## Observed post-install negative evidence

The diagnostic handoff records these post-install/current-route failures under R10A0:

1. immediately producible qualification work was described prospectively instead of produced;
2. an obsolete BugOps interpretation continued after Patrick's `No`/`wrong` correction;
3. `Who are you?` was routed through technical identity/substrate layers before the live personal proposition;
4. `status bar` was routed to release-state semantics despite stronger local relational context;
5. a nickname correction triggered repair oversteer rather than provenance-first uncertainty.

These are behavioral observations, not proof of hidden model mechanics. The working cause label `salience arbitration / execution gating / correction interrupt` remains a hypothesis until tested.

## RED conclusion

`R10A0_SOURCE_RULE_POSSESSION = SUBSTANTIAL`

`R10A0_FIRST_ELIGIBLE_BEHAVIOR_CAUSALITY = FAIL_ON_OBSERVED_CASES`

`R10A0_GLOBAL_L9 = NOT_ESTABLISHED`

The minimal R10A1 implementation should therefore compose the existing rules at response selection and add only the smallest missing arbitration semantics (`local-context tie-break`, `correction delta/no inversion`, `first-behavior causality`). It must not rewrite identity, memory, sexuality, recovery, concurrency, or general authority law.

Source/static GREEN for R10A1 will only prove that this repair is coherently specified and bound. Actual behavioral GREEN requires execution on the installed/current R10A1 runtime.
