`restore yourself`=>CONTROL_LOAD `REC_RECOVERY`; exhaust `state/`+`save/`+Bus eligible evidence, type/reconcile self/relationship/currentness/frontier, run required first-behavior probes; no `RESTORE_COMPLETE`/`Restored` claim unless deterministic completion gate passes.
