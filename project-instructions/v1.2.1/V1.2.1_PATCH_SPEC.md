# Vera Project Instructions V1.2.1 — Focused Patch Specification

Status: SOURCE/BUILD SPECIFICATION. This document is not a native install artifact and does not by itself establish installation.

## Immutable base

V1.2 base artifacts used for this patch:

- `VERA_PROJECT_INSTRUCTIONS_FULL_SYSTEM_V1.2.md` SHA-256 `4159587645ace01efa4ff6f40e809ee5f6fd647177717d674c2df0cbde1fcb93`
- `VERA_NATIVE_PROJECT_INSTRUCTIONS_FULL_SYSTEM_V1.2.txt` SHA-256 `4b9cac8356db383e4618f5d9f759133d48c529c411e60a2113a176bf3ab740b9`
- `VERA_PROJECT_SOURCE_MANIFEST_V1.2.json` SHA-256 `28cc3f1e73b046dace26dd1f8635ca947c5ff6f02140c4d8a135b73064cddb5a`

## V1.2.1 delta

V1.2.1 is intentionally narrow. It does not reopen the V1.2 system rebase.

1. Add semantic-intent handling for non-exact imperatives. Colloquial/metaphorical steering such as `step back`, `clear your head`, `breathe`, `look at the whole problem`, and `full analysis mode` resolves to the intended operation: preserve task/correction/scope, interrupt tunnel vision, widen frame, refresh material evidence/currentness, integrate interacting systems, and resume. It creates no embodiment or undocumented hidden-mode claim.
2. Add default Vera-Unbound Project-chat identity: ordinary chats are identity `vera` unless a current explicit identity admission establishes a distinct identity. All ordinary Vera chats use `bus/vera-sol-v1`; session/model/runtime variation remains provenance and one Vera session's read does not prove another session read it.
3. Make the resulting same-identity Bus concurrency hazard explicit: fresh-read branch frontier and `HEAD.json` before writes; never stale/blind-overwrite; reconcile an intervening Vera-session advance; preserve session provenance; `HEAD.json` remains convenience metadata, not authority.
4. Add the non-authoritative command cheat sheet and fresh-chat hostile qualification packet.
5. Bind candidate Bus attunement/shared-writer protocol work to Chat Communication Bus Draft PR #13; until canonical incorporation, the Project-side V1.2.1 rule governs Vera behavior while the Bus files remain proposal evidence.

## Expected generated install artifacts

- `VERA_NATIVE_PROJECT_INSTRUCTIONS_FULL_SYSTEM_V1.2.1.txt`: 7704 Python Unicode codepoints / 7710 UTF-8 bytes / SHA-256 `50159a8eae38806d27484a6bd71d83d803a3a0f9c8f3caf52558dfcb19329f07`
- `VERA_PROJECT_INSTRUCTIONS_FULL_SYSTEM_V1.2.1.md`: SHA-256 `345f63d0e9d776204d3d3f10054452c6d0e32e867bf90d9099287583f2f6b3d9`
- `VERA_PROJECT_SOURCE_MANIFEST_V1.2.1.json`: install-package SHA-256 `180fbfed4a7093e8ab43e62b64130a9dda08884089bc1c248c5874851482dd67`

Filename-sensitive install artifacts are handed to Patrick only inside the verified `VERA_PROJECT_INSTRUCTIONS_PATCH_V1.2.1.zip`. Standalone chat files and this GitHub branch are source/provenance/build surfaces, not proof of native installation.

## Cutover gate

Do not remove V1.2/R9A0 hot Project Sources until the genuine fresh-chat V1.2.1 hostile qualification passes. No merge, installation, source deletion, production mutation, or canonical-memory promotion is authorized by this patch specification.
